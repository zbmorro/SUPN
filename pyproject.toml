[build-system]
requires = ['hatchling']
build-backend = 'hatchling.build'

[project]
name = 'supn'
description = 'Shallow Universal Polynomial Networks'
dynamic = ['readme']
version = '0.1'
dependencies = ['torch']
authors = [{name = 'Zachary Morrow'}, {email = 'zbmorro@sandia.gov'}]
license = 'MIT' 

# [project.optional-dependencies]
# test = [
#     "pytest",
#     "pytest-cov",
#     "pytest-mock",
# ]

[project.urls]
Homepage = 'https://github.com/sandialabs/SUPN/' 

[tool.hatch.readme]
source = 'README.md'

# [tool.pytest]
# minversion = "9.0"
# addopts = ["-ra", "-q"]
# testpaths = [
#     "tests",
# ]
